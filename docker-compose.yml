version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8765:8765"  # Dynamically bind user ID as the external port
    environment:
      - USER_ID=${USER_ID}  # Pass user ID as an environment variable
    volumes:
      - ./cert:/app/cert
      - ./frontend:/app/frontend
      - ./src:/app/src
