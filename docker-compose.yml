version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "${USER_ID}:8765"  # Use the USER_ID environment variable as the external port
    environment:
      - USER_ID=${USER_ID}  # Pass user ID as an environment variable
    volumes:
      - ./cert:/app/cert
      - ./frontend:/app/frontend
      - ./src:/app/src
